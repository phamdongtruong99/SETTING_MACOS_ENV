export const formConfig = {
  validateMessages: {
    required: '${label} is required!',
    pattern: {
      mismatch: '${label} is not valid!',
    },
  },
  requiredMark: false,
};

export const appConfig = {
  title: 'Smartos Client',
};
